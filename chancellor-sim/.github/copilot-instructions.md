# Copilot Instructions for Chancellor Simulator

This directory inherits the canonical instructions from the repository root file:

- `/.github/copilot-instructions.md`

Use that file as the source of truth. Do not duplicate or locally override the project guidance unless there is a genuinely app-specific rule that cannot live at the root.
